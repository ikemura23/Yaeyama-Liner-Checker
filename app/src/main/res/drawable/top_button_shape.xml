<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ボタンが押されてない時 -->
    <item android:state_pressed="false">
        <!-- ボタンの形状 -->
        <shape android:shape="rectangle">
            <!-- ボタンの色 -->
            <solid android:color="@color/activity_top_annei_button"/>
            <!-- ボタンの枠線 -->
            <stroke android:width="1dp" android:color="#fffdfdfd"/>
            <!-- ボタンの角の半径 -->
            <corners android:radius="8dp"/>
            <!-- ボタン内の余白 -->
            <padding android:bottom="8dp" android:left="16dp" android:right="16dp" android:top="8dp"/>
        </shape>
    </item>
    <!-- ボタンが押された時 -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#82B1FF"/>
            <stroke android:width="1dp" android:color="#fffdfdfd"/>
            <corners android:radius="8dp"/>
            <padding android:bottom="8dp" android:left="16dp" android:right="16dp" android:top="8dp"/>
        </shape>
    </item>
</selector>